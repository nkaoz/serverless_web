packages:
  score:
    version: 1.0
    actions:
      credit:
        function: main.js # source for the action; in this case it is a folder
        runtime: nodejs:14
        web: yes # publish the action as a web action
        annotations:
          web-export: true
